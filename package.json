{
  "name": "rag-based-chatbot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "predev": "docker start rag_postgres || docker compose up -d",
    "dev": "concurrently --kill-others-on-fail --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "stop": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
